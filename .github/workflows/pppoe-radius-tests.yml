# Copyright 2026 Veesix Networks Ltd
# Licensed under the GNU General Public License v3.0 or later.
# SPDX-License-Identifier: GPL-3.0-or-later

name: pppoe-radius-tests

on:
  workflow_call:

jobs:
  pppoe-radius:
    name: PPPoE RADIUS Tests
    runs-on: self-hosted
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/test-setup
        with:
          topology: tests/06-pppoe-radius/06-pppoe-radius.clab.yml

      - name: Run PPPoE RADIUS tests
        run: |
          mkdir -p test-results
          robot --loglevel DEBUG --output test-results/output.xml --log test-results/06-pppoe-radius-log.html --report NONE tests/06-pppoe-radius/

      - uses: ./.github/actions/test-cleanup
        if: always()
        with:
          topology: tests/06-pppoe-radius/06-pppoe-radius.clab.yml

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pppoe-radius-test-results
          path: test-results/
          retention-days: 14
