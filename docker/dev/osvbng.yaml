# Auto-generated by osvbngd v0.0.4-3-g1968897-dirty
# Do not edit this header

logging:
    format: text
    level: info
    components:
        aaad: info
        arpd: info
        dpd: info
        ipoed: info
        pppoed: info
        sb: info
        subd: info
subscriber-groups:
    groups:
        default:
            vlans:
                - svlan: "100"
                  cvlan: any
                  interface: loop100
            address-pools:
                - name: subscriber-pool
                  network: 10.255.0.0/16
                  gateway: 10.255.0.1
                  dns:
                    - 8.8.8.8
                    - 8.8.4.4
                  priority: 1
            iana-pool: wan-link-pool
            pd-pool: subscriber-pd-pool
            dhcp:
                auto-generate: true
                lease-time: "3600"
            bgp:
                enabled: true
                advertise-pools: true
                redistribute-connected: true
            aaa-policy: default-policy
dhcp:
    provider: local
dhcpv6:
    provider: local
    iana_pools:
        - name: wan-link-pool
          network: 2001:db8:0:1::/64
          range_start: 2001:db8:0:1::1000
          range_end: 2001:db8:0:1::ffff
          gateway: 2001:db8:0:1::1
          preferred_time: 3600
          valid_time: 7200
    pd_pools:
        - name: subscriber-pd-pool
          network: 2001:db8:100::/40
          prefix_length: 56
          preferred_time: 3600
          valid_time: 7200
    dns_servers:
        - 2001:4860:4860::8888
        - 2001:4860:4860::8844
    ra:
        router_lifetime: 1800
        max_interval: 600
        min_interval: 200
interfaces:
    eth1:
        name: eth1
        description: Access Interface
        enabled: true
        bng_mode: access
    loop100:
        name: loop100
        description: Subscriber Gateway Loopback
        enabled: true
        address:
            ipv4:
                - 10.255.0.1/32
            ipv6:
                - 2001:db8:0:1::1/128
        lcp: true
protocols:
    bgp:
        asn: 65000
        router-id: 10.255.0.1
aaa:
    auth_provider: http
    nas_identifier: osvbng
    policy:
        - name: default-policy
          format: $remote-id$
          max_concurrent_sessions: 1
plugins:
    exporter.prometheus:
        enabled: false
        listen_address: :9090
    northbound.api:
        enabled: true
        listen_address: :8080
    subscriber.auth.local:
        allow_all: true
        database_path: /tmp/osvbng.db
    subscriber.auth.http:
        endpoint: http://host.docker.internal:8081/api/v1/auth
        method: POST
        timeout: 30s
        # tls:
        #     insecure_skip_verify: false
        #     ca_cert_file: /etc/osvbng/ca.crt
        # auth:
        #     type: bearer
        #     token: your-api-token
        # headers:
        #     X-Custom-Header: value
        response:
            allowed_condition:
                jsonpath: status
                value: "allowed"
        accounting:
            enabled: true
            start:
                endpoint: http://host.docker.internal:8081/api/v1/accounting/start
            update:
                endpoint: http://host.docker.internal:8081/api/v1/accounting/update
            stop:
                endpoint: http://host.docker.internal:8081/api/v1/accounting/stop
